name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: 9014680042
          password: sandeep@231

      - name: Build and Push Docker Image
        run: |
          if [[ ${{ github.event_path }} == "FirstApp" ]]; then
            docker build -t 9014680042/firstapp:poc FirstApp
          fi

          if [[ ${{ github.event_path }} == "SecondApp" ]]; then
            docker build -t 9014680042/secondapp:poc SecondApp
          fi

          if [[ ${{ github.event_path }} == "Modules" ]]; then

            docker build -t 9014680042/firstapp:poc FirstApp
            docker build -t 9014680042/secondapp:poc SecondApp
          fi
